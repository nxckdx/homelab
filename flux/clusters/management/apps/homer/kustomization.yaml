apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

namespace: homer

configMapGenerator:
  - name: config
    files:
      - config.yml=conf/config.yml

generatorOptions:
  disableNameSuffixHash: true